# ─── LLM Provider API Keys ──────────────────────────────────────────────
# At least one API key is required, depending on which model you use.

# Anthropic API Key (required for Claude models)
ANTHROPIC_API_KEY=sk-ant-xxxxx

# OpenAI API Key (required for GPT / o-series models)
# OPENAI_API_KEY=sk-xxxxx

# Google API Key (required for Gemini models)
# GOOGLE_API_KEY=AIza-xxxxx

# ─── Model Selection ───────────────────────────────────────────────────
# Default model alias or raw model ID. See `src/llm/models.ts` for registry.
# Examples: claude-sonnet-4, claude-opus-4, gpt-4o, gpt-4.1, gemini-2.5-pro
LLM_MODEL=gpt-5-mini

# Legacy alias (still supported, LLM_MODEL takes priority)
# CLAUDE_MODEL=claude-sonnet-4-20250514

# ─── LiteLLM Proxy / Custom Endpoints ─────────────────────────────────
# Set this to route all requests through an OpenAI-compatible proxy.
# LLM_BASE_URL=http://localhost:4000

# ─── Per-Agent Model Overrides ─────────────────────────────────────────
# Override the model for individual agents. Useful for cost optimization
# (e.g., cheaper model for research, stronger model for developer).
# RESEARCHER_MODEL=gpt-4o-mini
# PLANNER_MODEL=
# COPY_MODEL=
# DESIGNER_MODEL=
# DEVELOPER_MODEL=claude-opus-4
# QA_MODEL=gemini-2.5-flash

# ─── Puppeteer / Rendering ─────────────────────────────────────────────
# Chrome/Chromium executable path for Puppeteer (optional, auto-detected)
CHROME_PATH=

# ─── Pipeline Settings ─────────────────────────────────────────────────
# Max QA review loops before stopping (optional, defaults to 3)
MAX_QA_LOOPS=3

# Default number of slides (optional, defaults to 10)
DEFAULT_SLIDES=10
